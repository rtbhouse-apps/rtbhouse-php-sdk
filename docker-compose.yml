---

version: "3.2"

services:
  tests:
    build:
      context: .
      args:
        UID: "${UID}"
        GID: "${UID}"
    container_name: rtbhouse-php-sdk
    volumes:
      - ./src/:/home/apps/code/src
      - ./tests/:/home/apps/code/tests
